{% extends 'base.html.twig' %}

{% block title %}Passager index{% endblock %}

{% block body %}
    <h1>Passager index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Pays</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for passager in passagers %}
            <tr>
                <td>{{ passager.id }}</td>
                <td>{{ passager.nom }}</td>
                <td>{{ passager.prenom }}</td>
                <td>{{ passager.pays }}</td>
                <td>
                    <a href="{{ path('passager_show', {'id': passager.id}) }}">Voir</a>
                    <a href="{{ path('passager_edit', {'id': passager.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('passager_new') }}">Creer</a>
{% endblock %}
